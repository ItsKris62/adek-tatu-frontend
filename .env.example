# =====================================================================
# ADEK TATU FRONTEND ENVIRONMENT CONFIGURATION (.env.example)
# =====================================================================
# In development, points to the local backend API server (http://localhost:4000)
# In production, points to the production API origin
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
